Django>=4.1,<5.0
python-dotenv>=1.0.0
whitenoise>=6.6.0
gunicorn>=21.2.0
psycopg2-binary>=2.9.9  # Only if using PostgreSQL, remove if using SQLite
pillow>=10.2.0  # Required if using image uploads
django-tailwind>=3.6.0  # Only if using Tailwind in Django
